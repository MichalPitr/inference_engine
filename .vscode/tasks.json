{
	"version": "2.0.0",
	"tasks": [
		{
			"type": "shell",
			"label": "Nsight: autostart (localhost)",
			"command": "cuda-gdbserver ${config:host}:${config:port} ${config:executable}",
			"problemMatcher": [],
			"group": {
				"kind": "build",
				"isDefault": true
			}
		}
	]
}